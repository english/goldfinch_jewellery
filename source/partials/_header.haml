%header
  .image-container
    =image_tag("#{@header_image}_title.jpg")
  %nav
    %ol
      -for link in data.menu.links.each
        %li
          =link_to link, url(link), :class => current?(@title, link)
